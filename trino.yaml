services:
  trino:
    image: trinodb/trino:latest
    hostname: trino.devcon.io
    ports:
      - 8080:8080
    volumes:
      - ./trino:/trino:ro
      - ./conf.sh:/conf.sh:ro
      - ./init:/init:ro
    command: /init/trino.sh
